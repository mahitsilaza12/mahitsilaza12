const base_url = "/api"
export default {
    listClient  : `${base_url}/client`,
    addClient : `${base_url}/client`,
    getClient : `${base_url}/client/:id`,
    updateClient : `${base_url}/client/:id`,
    deleteClient : `${base_url}/client/:id`
}